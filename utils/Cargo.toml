[package]
name = "utils"
version = "0.1.0"
edition = "2021"

[dependencies]
colored = { version = "2.0.0", default-features = false, optional = true}

[features]
default = []
profile = [ "dep:colored" ]
